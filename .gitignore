# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Local Claude Code settings (per-machine, not shared)
.claude/settings.local.json

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Width-matrix output: the JSON summary is committed, the per-run CSV and the
# screenshots (~/Downloads/width-matrix) are not.
scripts/*.csv
scripts/width-matrix.result.only*

# Local-only notes (review tables etc.)
*.local.md
