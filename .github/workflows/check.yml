name: check

on:
  push:
    branches: [dev, main]
  pull_request:
    branches: [dev, main]

jobs:
  check:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm
      - run: npm ci
      # tsc -b && eslint && vite build && vitest run — the same gate as local `npm run check`.
      # Playwright is a devDep for local visual review only; it is not installed here.
      - run: npm run check
