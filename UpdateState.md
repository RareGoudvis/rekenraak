# UpdateState — Conversation History

Short summaries of what was discussed or changed in each session.
Most recent entry first.

---

**2026-05-31** — Round-5 splitsen/temperatuur fixes (build + lint green, app boots). **Splitsen mask bug**: `generateTotal`/`generateGiven` were double-counting decimal places (PLACE_VALUES already holds t/h/d) → Rooster now honors a t-only mask. **Positie masks + decimals**: splitsbenen/plaatswaarden/positietabel get a "Specifieke getalopbouw" mask under Maximum getal; the generator now *builds* the number from that mask (numFromMask), so "only TE,t" works; positietabel gained decimals (numberToDutchWords extended with "komma" for decimal digits) and its tables are right-aligned (word fills the left, no overlap). **Soorten** pills now lay out 2×2 (Benen row / Getal row). **Manual edit**: splitsen Geavanceerd section lists a textbox per generated number; typing one re-derives that exercise (pairs / place breakdown / words) via `recomputeSplitsenExercise` + `patchExercise`. **Temperatuur**: scale capped at 25 (glass continues a bit past, unlabelled) and negatives floor at −15; "Per rij" hidden for the verschil type (one exercise per row).

**2026-05-31** — Round-4 feedback fixes (build + lint green, app boots). **Preview**: reverted the auto-fit to width-only scaling + clip (no more distorted/compressed examples). **Font weight**: swept all viewer *content* to normal weight (solutions, answers, totals, fraction digits, etc.); only the opdracht title + structural table headers stay bold. **Config restyle**: added `pill` + `onOffRow/onOffBtn` helpers to sharedPluginStyles; Splitsen "Soorten"/"Notatie" are now themed pills, Getallenas/Ordening fraction toggles are label + Aan/Uit. **Splitsen**: "Splitsen (basis)" → **Rooster**; new **Decimalen (0–3)** setting + decimal place-value masks (t/h/d) for Rooster + splitsbenen + plaatswaarden (positietabel + verliefde harten stay integer); generator does scaled-int decimal decomposition, viewer formats via formatMathNumber. **Temperatuur**: removed the duplicate Variant toggle from the engine (variant = sidebar leaf); redesigned the thermometer SVG (glass gradient, bulb, minor+major ticks); new **Verschil** type — two thermometers with per-thermometer given-info (Gekleurd/Getal/Beide) + a "verschil = ___" answer (`|c1−c2|`).

**2026-05-30** — Round-3 polish + decimal/fraction depth (build + lint green, app boots; testing build for external feedback). **Preview**: `ExercisePreview` now measures + scales AND centers the example in its white box (translate offset; inner sized to max-content). **Sidebar/engine dedup**: reverted Splitsen to separate sidebar leaves (accordion, no wiskundig) and removed the layout picker from `SplitsenConfig`; removed the duplicate "Lay-out" toggle from `DeelbaarheidConfig`; nested Klok activity (Lezen/Tekenen/Omzetten) under Analoge/Digitale klok in the sidebar and dropped the Activiteit toggle from `ClockConfig`. Catalog disambiguates multi-parent variant labels (e.g. "Analoge klok · Lezen"). **Ordening decimals**: 1/2/3 decimal places (Decimalen control + mask shows t/h/d). **Getallenas**: decimal gets fine step presets (down to 0,001) + a validated custom-step text input; rational gets Gemengde getallen + Gelijknamige (niet vereenvoudigen) toggles; geheel got a minimum-bound slider and fixed descending/negative runs (clamped start math). **Base parity**: `baseDecimalPlaces` + `baseUnitFractionsOnly`/`baseAllowMixed` in Basisinstellingen, wired via `baseApply` (ordenenDefaults declares the keys). **Bug fix**: `digitAtPlace` now scales to integers so 3-decimal place masks extract digits without float drift.

**2026-05-30** — Big batch: preview/header fixes + Splitsen/Ordenen/Getallenas upgrades (build + lint green; needs in-app verification). **Shared**: new `VerticalFraction` component (extracted from MathBlockRenderer/FractionExerciseItem, repointed both); generic `patchExercise(blockId, field, exId, patch)` store action; `numberMatchesMask` + `digitAtPlace` in mathEngine. **A — fixes**: `ExercisePreview` now measures content + scales to fit a fixed box (ResizeObserver), so mass-add cards show the WHOLE example; mass-add grid → 2 columns (minmax 460) + taller preview. Sheet header (center title): Score box moved onto the Naam/Klas row (no longer floats below). **B — Splitsen**: layout selection moved out of the sidebar (single "Splitsen" leaf) into an inspector **dropdown** (wiskundig dropped); positietabel words right-aligned so tables align; splitsbenen gained a 30-vs-3T notation + a 4-combo (benen/getal × 30/3T) include-checklist that the generator mixes; plaatswaarden notation became letters/uitgebreid checkboxes mixed per exercise (new `SplitsenExercise.notation`). **C — Ordenen**: count up to 8; click-to-edit prompt numbers (re-sorts the answer via patchExercise); "specifieke getalopbouw" place masks for natural+decimal; rational shows vertical fractions with min/max denominator + stambreuken + gemengde-getallen toggles; gehele getallen got a lower-bound slider (−maxGetal…0). **D — Getallenas**: "Beide" arrow direction (mixed per set); decimal/rational/geheel number lines now real (generator precomputes `values: (number|Fraction)[]`, viewer renders an HTML label overlay with vertical fractions + nl-BE/negative formatting).

**2026-05-30** — Inspector/sidebar/help polish (build + lint green). **Geavanceerd** sidebar group ([BaseSettingsPanel.tsx](src/components/layout/BaseSettingsPanel.tsx)) is now a collapsed-by-default accordion. **Opdrachtblok** card ([Inspector.tsx](src/components/configurator/Inspector.tsx)) redesigned to sliders: Instructie (text, placeholder "Los op."), Score (slider 0…aantal×2, clamps when aantal drops), Aantal oefeningen (slider 1–20), Witruimte (renamed Spatiëring); locked mode disables all but Aantal. **Curriculum samenstellen** left list now shows **subdomain sub-headers** (groups by `item.context`, e.g. Geld → Herkennen/Bedrag tekenen/Wissel/Teruggeven) and the modal is wider (1280 / list 360). **HelpModal** rewritten into **Voor ouders** / **Voor leerkrachten** tabs; the teacher tab has a Basis block (original sections) + a Geavanceerd block documenting Basisinstellingen and Curriculum samenstellen.

**2026-05-30** — Round-2 refinements on the three workflow features (build + lint green; needs in-app verification). **Shared `ExercisePreview`** (new `components/shared/`, replaces MassAddPreview): builds a 1-exercise throwaway block, generates directly, renders the Viewer in a fixed-height clip box (`scale(.85)`, width-compensated) so previews are uniform. **Mass-add**: now 1 example/card + a top-level **domain filter bar** (chips from new `catalogDomains()`; `CatalogItem` gained `domainId`/`domainLabel`). **Basisinstellingen** enriched + moved: `BaseSettings` now carries per-place `baseOperand1Mask`/`baseOperand2Mask` + `baseBridges` map (replacing the single `baseBridgePolicy`); `baseApply` writes them cloned+guarded (only when non-empty). New **`BaseSettingsModal`** mirrors the real NaturalSettings UI (max presets, D/H/T/E getalopbouw, GEEN/MAG/MOET bruginstellingen via `getMaskPlaces`/`getBridgePlaces`). **Curriculum samenstellen** rebuilt as a proper authoring modal (`components/curriculum/CurriculumBuilderModal.tsx`): two-pane — left = every type grouped by domain with show/hide + bulk (per-domain + global) + variant select; right = the **real config plugin** (`EXERCISE_UI[typeId].Config`) rendered against an off-sheet **`draftBlocks`** store slice (new; `updateBlockSettings` falls through to it, no history/autosave) + a live example; footer emits the locked share link from the drafts. **Top bar** decluttered: **Delen** dropdown (Blad/Sjabloon) + **⋯ Meer** overflow (Exporteer/Importeer/Presets); removed the loose share/template/lock icons and the derive-from-sheet `handleShareCurriculum`. Both **Basisinstellingen** and **Curriculum samenstellen** now live in a sidebar **Geavanceerd** group under Thema (hidden in locked parent mode).

**2026-05-30** — Three workflow features shipped (build + lint green; needs in-app click + print verification). **(1) Global base settings** — new `src/config/baseSettings.ts` (`BaseSettings` {baseMaxGetal, baseNumberType, baseBridgePolicy} + `baseApply` that maps the semantic base onto each type's own keys via `'key' in defaults` guards: maxGetal/maxRange/maxNumber, numberType, bridges E/T/H/D; breuken deliberately excluded). New collapsible `BaseSettingsPanel` at the bottom of the sidebar. Snapshotted into each new block in `addBlockFromType` (order: registry defaults → base → leaf override), so changing the base never retro-affects existing blocks. Persisted in `WorksheetFile`/autosave/presets/share. **(2) Mass-add modal** — new `src/config/exerciseCatalog.ts` (walks APP_STRUCTURE, drops placeholders/non-registry, groups leaves by typeId into variant rows) + `MassAddModal`/`MassAddPreview` (live example per type via direct `REGISTRY.generate` → `EXERCISE_UI.Viewer`, no store writes; per-item error boundary + IntersectionObserver lazy render + re-roll; variant toggles), opened from a new "Toevoegen" TopBar button; "Alles toevoegen" batch-adds then generates. **(3) Locked curriculum templates** — `WorksheetFile` v2 gains optional `curriculum: {locked, allowedTypes[{typeId,label,lockedConstraints}]}`; store `curriculum` state enforced at the single choke point (`updateBlockSettings` allow-lists only numberOfExercises+pageBreakBefore when locked; `updateBlockInstruction`/`updateBlockLayout` frozen); sidebar swaps to a flat whitelist palette (hides search + base panel), Inspector shows a lock banner + hides config/differentiation/advanced (keeps count + Genereer), new "Deel curriculum" TopBar action derives the whitelist from current blocks and shares a template link; App.tsx adds a locked-load confirm. Lock survives refresh (autosave carries curriculum).

**2026-05-30** — Five new exercise families (basic versions, refine later), all via the registry contract. **#4 ordenen** (`ordenen`, 4 numberType leaves nat/dec/rat/geh): order 3–5 numbers with `<`/`>`/both. **#2 deelbaarheid** (`deelbaarheid`): tick-table (rows × selected divisors) + veelvouden fill-row. **#3 splitsen place-value** (extends `splitsen`, now an accordion banner): new layouts `positie-tabel` (Dutch number-word → D…E digit grid, via new `numberToDutchWords` helper ≤1e6), `positie-benen` (legs → H/T/E, toggle which side blank), `positie-math` (letters `7H+9T+2E` / expanded `300+70+8`, decompose/compose), place values up to 1e9. **#6 getallenas** (`getallenas`, natural only; dec/rat/geh stay placeholders): SVG number line with arrow-direction + easy/hard blank modes, step/max/ticks knobs. **#8 temperatuur** (new "Temperatuur" subdomain under Meten en metend rekenen): SVG thermometer, variants `kleuren` (colour to a temp) / `aflezen` (read a coloured meter), negatives toggle (−20…30). Each = generator + viewer (FragmentableGrid) + config + registry/UI/appstructure rows. Build + lint green; needs in-app + print verification.

**2026-05-30** — Print footer pinned to page bottom + sticky TopBar. Footer (`<tfoot>`) was floating under the last block on a short last page; added a `height:100%` chain (html→body→#root→.print-root→.print-main→.print-area-shell→`.print-area` table) in the `@media print` block so the table fills the page, the tbody cell absorbs the slack, and `<tfoot>` sinks to the page bottom. TopBar wrapper made `position:sticky; top:0` (it's a direct child of the `overflowY:auto` center column) so the crucial toolbar stays visible while scrolling the preview. Build + lint green; needs a print check (watch for a stray blank page on near-exact-fit sheets).

**2026-05-30** — Shipped **v0.4**. Bumped `RELEASE_VERSION` → v0.4 (+ Dutch banner summary on the print improvements) and package.json → 0.4.0. Rebuilt README.md (kept the Dutch teacher intro, added an English "Architecture — how it fits together" section: 3-panel data flow, the exercise registry, store, generator contract, print system, persistence). Refreshed the now-accurate print docs in ARCHITECTURE.md §9 (+§8/§11/§12) and CLAUDE.md to the real-`<table>` repeating header/footer + `@page margin:0` + FragmentableGrid system. Committed the whole working tree (registry restructure + print rewrite + docs), fast-forwarded `main` to `dev`, tagged `v0.4`, pushed main/dev/tag to origin.

**2026-05-30** — Exercises now flow across page breaks + header field alignment. Multi-item viewers each put all items in one `display:grid`, and Chrome does not fragment a grid container across printed pages — so a block taller than the remaining space jumped wholesale to the next page (reported for "Breuk van een lijnstuk" and "analoge klok"). Added a shared `FragmentableGrid` (src/components/viewer/FragmentableGrid.tsx): a block container holding per-row grids, each row `break-inside:avoid` (`.print-row`) so rows never split but the block breaks between them — the same row-chunking pattern CijferViewer already used successfully. Converted all grid-based viewers (Clock, Fraction, MathBlockRenderer, Geld, GeldTekenen, GeldWissel, Mab, Splitsen basic+mathematic), preserving each one's columns/gaps/justify/align; flex-column/flex-wrap viewers (Cijfer, GeldTeruggeven, Splitsen verliefde-harten) already fragment and were left alone. Header: `renderFields` gained an `align` param so the fill-in fields sit opposite the title — title-left now right-aligns Naam/Klas/Nr/Datum (title-right/center unchanged). Build + lint green; needs a Chrome/Edge print check.

**2026-05-30** — Print footer/header now repeat on EVERY page (real `<table>` markup). The prior div-based `display:table-header-group`/`table-footer-group` worked in Firefox but Chrome/Edge (Blink) only repeats `<thead>`/`<tfoot>` across printed pages for **real** table elements — so the teacher's PDF showed the footer only on the last page and the repeat-header strip only on page 1. Rewrote the print card in App.tsx as a shell `<div>` (keeps the screen card look + `a4Sheet`, holds `a4Ref`, and is the positioned container for the page-break indicators, which moved out of the table) wrapping a real `<table class="print-area">` with `<thead>` (spacer + repeating name strip), `<tbody><tr><td class="print-body-cell">` (header + blocks, 16mm side padding on the cell), and `<tfoot>` (school left / vrije tekst right). Screen CSS flattens the table to block flow (display:block on table/tbody/tr/td, thead/tfoot hidden) so the preview is unchanged; print CSS uses native table groups and `.print-area-shell { display:block !important }` to override a4Sheet's inline flex (flex must never reach the `<table>` or Chrome drops `display:table` and stops repeating). Real spacer `<div>` replaces the `::before`; `.print-body-inner` dropped (the `<td>` carries padding). Build + lint green; needs a final Chrome/Edge print check.

**2026-05-30** — Robust print rewrite: real margins + repeating non-overlapping footer. Teacher's printed PDF showed `@page` margins ignored (content at paper edge), the `position:fixed` footer overlapping/cutting exercise bottoms, and a page number where vrije tekst should be — all because the layout depended on `@page` margins that the print dialog ("Margins: None/Minimum") silently zeroes. Switched to the CSS table running-header/footer technique: `.print-area` becomes `display:table` in print with `.print-thead` (table-header-group, top-margin spacer), `.print-body`/`.print-body-inner` (table-row-group, 16mm side padding), and `.print-tfoot` (table-footer-group, school-left / vrije-tekst-right). Browsers repeat header/footer groups on every page AND reserve their height, so margins and footer survive any dialog setting and content never overlaps. `@page` margin set to 0; usePrint.ts still blanks the browser margin boxes. Body wrappers are `display:contents` on screen so the preview is unchanged. Voettekst menu: dropped Paginanummering (Chrome can't count pages), moved Vrije tekst to the right zone. New "Koptekst op elke pagina herhalen" toggle (`HeaderData.repeatHeader`) extracts the name fields into a `renderFields()` helper and repeats them in `.print-thead` on every page (page-1 body copy hidden to avoid duplication). Build + lint green; needs a manual Chrome/Edge print check.

**2026-05-30** — Fix print footer + page-width regression from the print overhaul. The footer had been moved into `@page` margin boxes (`@bottom-center { content: counter(page) }`), but Chromium ignores margin-box `content`, so Chrome/Edge printed blank page bottoms. usePrint.ts (blank-boxes-only) and index.css (16mm sides, `.print-footer-bar` fixed rule, `.print-area` bottom padding) were already corrected in a prior pass; restored the missing HTML footer in App.tsx — re-added the `footerData` store selector and the print-only `.print-footer-bar` div (school | klas | leerkracht left, vrije tekst right, `space-between`). No live page number (Chrome can't count pages from HTML/CSS without a paginator). Build + lint green.

**2026-05-30** — Share-link compression + print/layout overhaul. Share: links now LZ-compressed via lz-string (`compressToEncodedURIComponent`, ~6–8× smaller) with cap raised 6000→30000; old base64 links intentionally dropped (alpha). Print/layout: new global "Ruimte tussen oefenreeksen" slider (`DocSettings.blockSpacing`); preview widened 800→920px; print side margins 18→14mm, top 15→18mm for less-cramped continuation pages; sets may now split across pages (`break-inside: avoid-page` removed) while opdracht line stays glued to its first row (`.print-opdracht break-after:avoid`) and single exercises never split; per-block "begin op nieuwe pagina" hover toggle (`MathBlock.pageBreakBefore` → `.page-break-before { break-before: page }`) with on-screen "↡ nieuwe pagina" marker; real per-page footer (centered `counter(page)` + school info) injected as `@page` margin boxes by usePrint.ts, replacing the hardcoded "Pagina 1" HTML footer. Build + lint green.

**2026-05-30** — Lint cleanup: cleared all 41 pre-existing eslint errors, no behaviour change. mathEngine dead initializers (`let d1=1,…`) → typed uninit decls; `op1: any` → `number | Fraction`, `op2` let→const; `value: any` → `unknown` in 6 config plugins; `isFraction` param `any`→`unknown`; `MathBlock.constraints` kept `any` with a documented eslint-disable; AlphaPopup lazy `useState` init (dropped the effect); eslint-disable for the boot-time autosave `setState`; eslint config override adds `argsIgnorePattern`/`varsIgnorePattern: '^_'` for intentionally-unused args. Build + lint both green.

**2026-05-30** — Central registry refactor: replaced the scattered `typeId` if-else wiring (dispatch, Inspector config mount, App viewer routing, `addBlockFromType` defaults) with a single exact-key registry — `config/exerciseRegistry.ts` (pure data: generator/field/defaultConstraints/defaultCount) + `config/exerciseUI.tsx` (Viewer/Config). Store gained a generic `setExercises(id, field, data)` action replacing the 9 per-type setters; `generateDispatch` is now a registry lookup. Added per-family constraint interfaces to types.ts. Normalized viewers to a uniform `{block, showSolutions}` (MabViewer derives mode from typeId; new ClockViewer/FractionViewer wrappers hold the grid logic moved out of App.tsx). Build + lint green. Adding a type is now: generator + viewer + config + one row in each registry file.

**2026-05-30** — Added ARCHITECTURE.md (full system map: data flow, add-a-type contract, generator contract, per-typeId registry table, print/persistence) and brought the stale CLAUDE.md back in sync (removed the dead react-pdf/WorksheetPDF section, updated dir tree + types table + add-type steps for MAB and Geld Wissel/Teruggeven).

**2026-05-27** — Print + Geld polish. Extended `@page` rule with empty `@top-*`/`@bottom-*` margin-box content so Chrome/Edge stop injecting URL/date headers and page-of-Y footers (Firefox still ignores). Tagged per-exercise wrappers in every viewer (Geld/GeldTekenen/GeldWissel/GeldTeruggeven/Mab/Splitsen/Clock + FractionExerciseItem wrapper in App.tsx) with `.print-exercise` so cells never split mid-block; `.print-block` got `break-inside: avoid-page` as soft hint. Bedrag tekenen `Voorbeelden tonen` bar shrunk (Bill 42×24, EuroCoin 26, CentCoin 22, label 9px) so all 13 denominations fit on a single line. Dropped `Hoeveelheid` (D/H/T/E) toggle from Geld herkennen — removed config section, viewer read sites, and `showPlaceValues`/`placeValues` defaults. Geld herkennen denominations defaults shrunk (Bill 56×32, EuroCoin 36, CentCoin 30 via new `width/height`/`size` props on the shared SVG components). New `Geldlayout: 'samen' | 'gescheiden'` constraint + Differentiatie toggle (herkennen only) — `gescheiden` splits bills+euro-coins onto row 1 and cent-coins onto row 2 separated by 10px gap.

**2026-05-27** — MAB v3 polish: symbolic tens rotated to horizontal bars stacked vertically (matches MAB modes, fixes wide-T-column bug). Units render in a deterministic column-first 2-row "domino" pattern (1, 2, 3, 4, 5…) for subitizing — applies to both symbolic dots and Dienes cubes. `MabStyle` renamed: `symbolic` | `mab-bw` (old `realistic`) | `mab-color` (new — units yellow, tens green, hundreds red, thousands blue; black strokes throughout). Back-compat at read sites so old saves with `'realistic'` resolve to `'mab-bw'`. Hundreds-cell bumped to 7px (flat 70×70) so the hundreds glyph reads as the biggest in MAB modes; thousands cell matches.

**2026-05-27** — MAB v2 + tekenen + sidebar branding. Scaffolding model rebuilt as `scaffolding: 'positietabel' | 'kader' | 'geen'` (was `showBox: boolean`); control moved out of MabConfig into the Differentiatie card; backwards-compat fallback at read sites. Geavanceerd accordion hoisted from MabConfig into the shared Inspector accordion (matches cijferen/geld pattern); contains Oefeningen per rij + Tekenvak hoogte + Antwoordlijn hoogte. `boxHeight` default 100→60 (slider min 40). `Tussennullen` toggle + `allowInternalZero` constraint + `hasInternalZero` helper removed — mask now sole structural control. Bug fix: `Tot N` and mask buttons clear `mabExercises` so the preview shows the empty-state prompt instead of stale out-of-range numbers. New `mab-tekenen` (Getallen tekenen) exercise: reverse of herkennen — number printed on the answer line, student draws blocks; shares generator, config, and viewer with herkennen via a `mode` prop. `Geen` scaffolding only offered for herkennen (tekenen always keeps the drawing target). Realistic glyphs: `CELL` 4→6 (60×60 hundreds flat), tens rotated to horizontal bars stacked vertically so all H/T/E columns share fixed width. Sidebar header reworked: logo left, editable site title + subtitle right (subtitle placeholder `using Basisonderwijs Vlaanderen`), both persisted to localStorage keys `enderklas_site_title_v1` / `enderklas_site_subtitle_v1`.

**2026-05-27** — MAB box redesign: drawing now splits into left-to-right H/T/E columns inside one optional box with H/T/E header labels + dividers; single always-shown answer line below. Replaced `scaffolding`/`boxBorderColor`/`useSpecifiek`/`specifiekeGetallen` constraints with `showBox` + `answerHeight`; H/T/E labels bumped to 14px centered. Per-place glyphs split out of `MabBlocksSVG.tsx`; viewer now flex-wraps glyphs per column. MabConfig adds Geavanceerd accordion (Oefeningen per rij + Tekenvak hoogte + Antwoordlijn hoogte sliders) and Met/Zonder kader toggle; Inspector cleared of MAB-specific scaffolding/exercisesPerRow. Block toolbar (lock/duplicate/trash/up/down) relocated to outside-right of the dashed border, vertically stacked. Header gained `fieldOrder` + `fieldWidths` (both optional for back-compat); App.tsx renders fields via `fieldOrder` loop with per-field width, Inspector replaces 4-checkbox grid with reorderable rows (hover ↑/↓ buttons) + per-field length sliders. Centered title position with no title now positions score in flow (`justify-content: flex-end`) so it can't overlap the header answer lines.

**2026-05-27** — Teacher-quality-of-life batch (eight items): (1) Auto-save to `enderklas_autosave_v1` localStorage key, debounced 1.5s via module-level zustand subscribe; boot-time banner offers restore when current sheet is empty. (2) Named preset library (`enderklas_presets_v1`, max 20) with new `PresetModal` (save current + list with Laad/× per row). (3) `document.title` follows `headerData.titel`. (4) Sidebar search filters APP_STRUCTURE tree by label and auto-expands matches. (5) `duplicateBlock` store action + 🗐 icon in block-control overlay (deep-clones constraints + exercises, resets `locked`, switches activeBlock to clone). (6) Donate ❤ link in sidebar footer pointing at Buy Me a Coffee (placeholder URL `buymeacoffee.com/REPLACE_ME` — TODO replace before merge). (7) Release update-banner driven by `RELEASE_VERSION`/`RELEASE_SUMMARY` in new `src/config/version.ts`; dismissed-version cached in `enderklas_release_seen_v1`. (8) Werkbundel via deelbare URL — base64 in `#share=…` hash (not query, so it doesn't leak through server logs); 🔗 Deel button copies link, 6 KB cap with fallback alert, boot-time hash detection with confirm-replace. HelpModal expanded to cover all new features.

**2026-05-27** — Document-wide feature batch: (1) `Genereer alles` in TopBar regenerates all non-locked blocks; new shared `services/generateDispatch.ts` is now single source of truth for typeId→generator routing (Inspector uses it too). (2) Per-block 🔒 lock toggle in block-control overlay + `locked` flag on `MathBlock`; locked blocks skipped by bulk regen. (3) JSON Exporteer/Importeer (`services/persistence.ts`) — versioned schema (v1) covering blocks/header/footer/docSettings; import confirms-and-replaces. (4) Doc-wide `numberBlocks` toggle in Opdrachtinstellingen renders `1.`/`2.`/… prefix on block headers (render-time, doesn't mutate instructionText). (5) Theme system moved into store with three themes (dark/light/colorblind); ☀/☽ sidebar toggle replaced by 3-way Thema card in Inspector; `[data-theme="colorblind"]` block in theme.css uses IBM CB-safe palette. (6) Contact (X-link) + Help icon buttons added to sidebar footer; new `HelpModal` component with sectional Dutch usage guide (cloned from AlphaPopup pattern, follows theme variables).

**2026-05-27** — Added MAB > Getallen herkennen exercise type under Getallenkennis > Getalbegrip. New `MabExercise` type + `mabExercises` field on `MathBlock`. Generator at `services/mab/mabGenerator.ts` supports random-with-mask and manual-number-list paths, optional internal-zero suppression. Viewer renders place-value blocks via new `MabBlocksSVG` component with two styles: 'symbolic' (dot/bar/square) and 'realistic' (Dienes 1×1/1×10 rod/10×10 flat/10×10×10 cube). Scaffolding: answer 'lijn' (dotted line) or 'tabel' (H/T/E columns with row). Config plugin exposes style, maxNumber (10/20/100/1000), mask, manual list, internal-zero toggle, box border color, box height. Sidebar entry added as MAB accordion with 'Getallen herkennen' leaf + 'Getallen tekenen' placeholder.

**2026-05-27** — Print hid worksheet behind mobile-block overlay because print preview narrows the viewport and re-triggers the `max-width: 768px` media query; added `.mobile-block { display: none !important; }` inside `@media print`.

**2026-05-27** — Multiple cijferen mask fixes: Getal 1 multiplicand mask fell back to random when applyMask failed range check (hasMask0 guard added); Getal 2 multiplier/divisor masks ignored decimal keys (t/h) because applyMask was called with dp=0 — fixed to pass dp so fractional multipliers/divisors work; header display for fractional operands now uses dp instead of rounding to integer; AlphaPopup wired into App.tsx; mobile/tablet block added; @page margin-top:15mm for consistent top margin on new pages; print-block no longer has break-inside:avoid (blocks can split, individual exercises cannot); partial products in multiplication hidden unless showSolutions; answer rows in add/sub and multiplication moved directly below thick line (removed gap row).

**2026-05-27** — Multiple fixes: header writing lines expand to full width; print produces multiple pages (root cause: `overflow: hidden` + `height: 100vh` on html/body/#root overridden in print CSS); cijferen division quotient columns now always match dividend digit count; multiplication Getal 2 mask was overridden by `minMultiplier` clamp — fixed so masked multiplier only clamps to ≥2; print exercise page-break-inside avoidance added at block and exercise level.

**2026-05-26** — Teruggeven config polish: diagram font/size tuned, middle node shows € + blank line in ingevuld mode, Centpatroon updated to Belgian-only options (25ct/10ct/5ct), added "Leeg" scaffolding level (no diagram, just answer field), Prijsbereik moved to preset buttons with auto-updating betalen-met options.

**2026-05-26** — Added Geld > Wissel (bill + drawing box) and Geld > Teruggeven (change-giving arrow diagram with 5 scaffolding levels, 2 answer types) as new active exercise types; renamed "Tekenen" to "Bedrag tekenen" and removed "Gepast betalen" placeholder.

**2026-05-26** — Rewrote CLAUDE.md with full codebase documentation (directory tree, all 8 exercise types, data flow, step-by-step guide for adding new exercise types, code commenting guidelines for junior devs). Created UpdateState.md and added Stop hook to auto-remind about session summaries.
