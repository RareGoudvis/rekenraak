# UpdateState — Conversation History

Short summaries of what was discussed or changed in each session.
Most recent entry first.

---

**2026-06-05** — Bugfix + render batch. **OOM root cause:** a leftover base/grade `maxGetal` with no matching dropdown option rendered as "—" and reached generators unclamped → memory overflow (also what made the even/oneven rooster *look* like a print bug). Fix: opt-in `clampToLowest` prop on [PopupSelect](src/components/ui/PopupSelect.tsx) snaps an out-of-range value to the lowest option and persists it; added to every numeric max dropdown across ~18 config plugins. **Rooster** ([EvenOnevenViewer](src/components/viewer/EvenOnevenViewer.tsx)): switched from width-based flex-wrap to a fixed `perRow`-column grid so cells align and "Getallen per rij" is honoured. **Breuk van een lijnstuk** ([FractionExerciseItem](src/components/viewer/FractionExerciseItem.tsx)): berekeningslijnen now read `___ cm : ___ = ___ cm` / `___ × ___ cm = ___ cm`; matching cm hint in the [Inspector](src/components/configurator/Inspector.tsx) scaffolding picker (lijnstuk only, not hoeveelheid-abstract).

**2026-06-04** — Curriculum + style batch. **Leerjaar mapping** grounded in the Flemish wiskunde leerplan: [gradePresets.ts](src/config/gradePresets.ts) now resolves each leaf's minLeerjaar as max(numberType, typeId, label, explicit) with leerplan-correct grades (gehele→L4, breuken→L2, decimale→L4, cijferen→L3, romeinse→L5, …) and leerplan max ceilings (L4≤10 000, L6≤10 miljard). **Sidebar regroupings** ([appstructure.ts](src/config/appstructure.ts)): even/oneven lifted out of Deelbaarheid, Veelvouden+Deelbaarheid merged, time subdomeinen merged into "Tijdstip en tijdsduur", Meetkunde reordered before Meten, and fraction +−×: (hr-std-*-rat) moved into a new Bewerkingen›"Bewerkingen met breuken" subdomein (tagged L4). **Style builder**: new [StyleBuilderModal](src/components/configurator/StyleBuilderModal.tsx) (size/weight, colors, lines, fill, padding, alignment per header/opdracht-titel/footer with a curated print-safe palette + live preview), driven by optional `RegionStyle` objects in `docSettings` (persist via existing autosave), applied through [regionStyle.ts](src/services/regionStyle.ts)'s `overlayRegionStyle`; dropped the footer `!important` on color/size so custom wins. **Mobile block**: now a teaser with a looping desktop demo ([public/rekenraak-demo.mp4](public/rekenraak-demo.mp4)) + warmer why-copy.

**2026-06-04** — Large inspector consistency + UX batch. Fixed the stepped ("Stappen") layout so the operand sits on the first answer line. Added shared primitives — [PopupSelect](src/components/ui/PopupSelect.tsx) (dropdown for value pickers), [StylePicker](src/components/configurator/StylePicker.tsx) (visual-variant modal gallery, wired to MAB Stijl), [BridgeControl](src/components/configurator/BridgeControl.tsx) (carry-arrow 'bruggetje' diagram replacing the per-place GEEN/MAG/MOET grid in addition/cijfer + base modal), [InfoTip](src/components/ui/InfoTip.tsx) + [SettingLabel](src/components/configurator/plugins/SettingLabel.tsx) (one-line ⓘ help on every setting), plus `divider`/`sectionBox`/`select`/`hint`/`miniLabel`/`numInput` style tiers. Swept ~18 plugins: range pickers → dropdowns (`Tot {n}`), `Maximum bereik`→`Maximum getal`, getalopbouw-before-bruggen order + dividers, uniform label weight. TopBar: new `secondary` button variant (Toevoegen demoted), "Nieuw blad" reset, autosave now **auto-resumes** silently (prompt removed) and persists `selectedGrade`. New soft **leerjaar** selector (1–6) in the sidebar: [gradePresets.ts](src/config/gradePresets.ts) seeds base difficulty + filters leaves by grade (rat≥L2, geh≥L3, dec≥L4).

**2026-06-04** — Two opdracht-titel UI fixes: a Kader-styled titel with a MAG/MOET/★ prefix no longer double-frames (prefix now renders as plain bold text + a vertical rule inside the box instead of its own bordered pill); added a "Standaardtekst…" dropdown under the Instructie field (new [instructionPresets.ts](src/config/instructionPresets.ts), generic verbs + per-type suggestions) so teachers click a common instruction instead of retyping.

**2026-06-03** — Post-ship polish: getalpatronen rows now use a 1fr grid (columns align across rows) with bottom-aligned connectors so the `–` sits on the number line. Getallenrijen gained a **Kader tonen** toggle (Differentiatie, default on) to drop the rounded frame. Deelbaarheid (kleuren) sidebar/mass-add variants are now the **view modes** (Rooster / Omcirkelen / Kleurraster) instead of divisor groups (divisors stay configurable); config segment relabelled Markeren → Omcirkelen.

**2026-06-03** — Shipped "Getalbegrip uitbreiden" to dev + main: bumped the sidebar "Updated" date to 03/06/2026.

**2026-06-03** — Fixed deelbaarheid-kleuren: the divisor is now **random per row** (was a fixed rotation that never changed on Genereer), and the **kleurraster** holds **non-repeating random numbers** with a configurable Maximum getal instead of a static 1..N (so the coloured answer actually varies). Markeren numbers now align in a grid; raster cells widened for up to 4-digit numbers.

**2026-06-03** — Getalpatronen refinements: the `+/−` getalopbouw now spans the full place range from the block maxGetal (D/H/T/E, plus t/h/d for decimals) instead of just T/E; added a **Max decimalen (1/2/3)** selector for decimal patterns (generator precision follows it); **Operatoren invullen** gained an **Operatorweergave** toggle (Enkel teken `+` vs Volledig `+9`). Also bumped the sidebar wordmark slightly larger (height 38).

**2026-06-03** — Two new families + default tweak. **Getalpatronen** (Patronen subdomain, nat/dec/geheel): a row that follows a 1–4-step operation cycle (`+ − × :`) from the left; teacher selects which operations may occur, each with its own operand max + getalopbouw; differentiation toggles draw arrows + a write-line and can pre-fill N operators (slider). Plain line with `–`/arrow connectors, no frame. New `PatroonExercise`, `patroonGenerator.ts`, `PatroonViewer`/`PatroonConfig`. **Deelbaarheid (kleuren)** (under Deelbaarheid): colour/circle the multiples of a divisor in three view modes — rooster (strip), markeren (omcirkel), kleurraster (honderdveld) — with a `Rest berekenen` toggle (`rest: ___`); leaves door 2/5/10 · 4/8 · 3/6/9 · gemengde reeksen (toggle 2–12). New `DeelbaarheidKleurExercise`, generator, viewer/config. **Defaults**: scores + scheidingslijn off, opdracht-nummering on, voettekst fully off. Docs synced; lint + build clean.

**2026-06-03** — Meten/omtrek layout dynamics: `MetenViewer` now picks **1 or 2 columns** based on whether two items fit the print width (`A4_CONTENT_PX = 625`), so long lines/large shapes no longer overflow the sheet. Fixed the `___ cm bij elke zijde` labels by anchoring each outward (transform follows the side normal) so they never overlap the figure, with wider padding when the scaffold is on. Hid the per-side scaffold toggle for **lengte meten** (pointless for a single line) — omtrek only.

**2026-06-03** — Polished the meten/omtrek exercises. **Lengte meten**: the `gegeven` model became a **juist/fout** exercise (line drawn to scale + a stated length the pupil verifies by measuring, circles juist/fout; `claim`/`claimCorrect` on `MeetExercise`); the meten path dropped its per-item "Lengte =" prefix and right-aligns the second column. **Omtrek**: shape constructors now return exact `sides[]` so *hele cm* never drifts to 0,9/1,1; length control relabelled "Lengte per zijde"; bigger label padding so labels don't clip the figure. **Both**: the `___ cm bij elke zijde` scaffold, the answer-mode (Eén lijn / Som van zijden) and a new **answerUnit** (Met cm / Enkel lijn) moved into the Inspector **Differentiatie** card. Docs synced; lint + build clean.

**2026-06-03** — Two batches. **Breuken getalopbouw consistency**: vergelijken representaties now swaps a breuk side's getalopbouw to the teller/noemer widget (`FractionMaxField`) and stores a real `aFrac`/`bFrac`; getallenrijen rational gets the same widget (noemer=sprong, teller=maxTeller) and the decimal mask now matches the step's decimal precision; gelijknamig gained a **Doelnoemer** (fixed common noemer, else KGV) + max-noemer; `BreukBewerkViewer` re-laid-out as a grid so `=`/`→` and writing lines align. **New geometry** under Meten → Lengte en oppervlakte: **lengte meten** (straight line / 0–4-corner polyline) and **omtrek** (driehoek · vierkant/rechthoek/ruit/parallellogram/trapezium/vierhoek · vijf-/zes-/zeven-/achthoek · cirkel-met-middelpunt). Drawn to real cm scale (`MetenViewer`, 1cm≈37.8px), with a meet-model toggle (op-schaal vs lengtes-gegeven), cm/mm precision, per-side `___cm` scaffold, and single-line vs sum-of-sides answer. New `MeetExercise`/`meetExercises`, `metenGenerator.ts`, `MetenViewer`/`MetenConfig`. Also **hid the Probleemoplossend denken domain** from the sidebar (added `Domain.hidden`). Docs synced; lint + build clean.

**2026-06-03** — Refined the round-1 breuken/komma exercises after teacher testing. **Vergelijken → representaties**: restored per-side *Specifieke getalopbouw* (new `leftMask`/`rightMask`, honored by the generator) and reworked the config into 2 columns (Linkerkant │ Rechterkant, divider) each owning its representation + mask. **Breuken bewerken**: extracted the addition "Breuk 1" teller/noemer control into a shared `FractionMaxField.tsx` and used it for gemengd + vereenvoudigen getalopbouw (`maxNumerator`/`maxDenominator`); answer box → single writing line. **Vereenvoudigen**: 2 presets (binnen de maaltafels `tablesOnly` / grotere breuken) + an `allowIrreducible` toggle that mixes in already-simplest fractions. **Verliefde harten** print bug: routed hearts through `FragmentableGrid` (fixed columns) so screen and print match and hearts fragment across pages. Docs synced; lint + build clean.

**2026-06-03** — Added five exercise types/variants. **Splitsen → Splitsboom**: new `layout` on the splitsen typeId — a single split-tree (top + two legs) where the blank slot is random among the teacher-ticked positions (top/left/right), maxGetal ≤ 1000, getalopbouw masks for top + sides. **Getallenrijen** (new subsection under Getalbegrip, 4 number types): number sequences shown in a pill with dotted blanks — getallenas minus the axis line, with a custom jump for every type, a getalopbouw mask on the anchor, and stijgend/dalend direction; reuses the `getallenasExercises` field + `GetallenasExercise` shape with its own generator/viewer/config. **Vergelijken → Breuken & kommagetallen**: new `representaties` subType comparing two tienden/honderdsten values, each side rendered as breuk/kommagetal/plaatswaarde (5E4t)/woorden (5 tienden) via `RepValue` + `representations.ts`. **Breuken bewerken** (new typeId, 3 leaves): gemengd↔breuk (direction toggle), gelijknamig maken (LCM), vereenvoudigen — reuses exported `gcd`/`simplifyFraction`/`toMixedNumber`. **Breuken rangschikken**: replaced the placeholder — orders 2–5 fractions (stambreuken/gelijknamige/gelijknamig-te-maken/speciale curated pool), reuses `ordenenExercises` + `OrdenenViewer` with a new generator/config. Docs (ARCHITECTURE/CLAUDE) synced; lint + build clean.

**2026-06-02** — Rebranded the app **Enderklas → Rekenraak**: new sidebar header (rekenraak wordmark inline-SVG that adapts per theme — `reken` = `--text-main`, `raak` = `--accent` — with a static "Gemaakt door Ruben V.H." line; the old editable site title/subtitle inputs + their localStorage were dropped). The logo is clickable → opens a new stubbed `AboutModal` ("Over dit project"). Renamed all `enderklas_*` localStorage keys to `rekenraak_*` (one-time local reset of autosave/presets/tour/pins), installed the new "rr" favicon set into `public/` + rewired index.html, retitled the tab/banner to Rekenraak, set `package.json` name to `rekenraak`, and deleted the orphaned `enderklas-logo.png` + old `favicon.png`. Docs updated (README/ARCHITECTURE/CLAUDE). Live URL `app.enderklas.be` left as-is pending a domain. Fleshed out the `AboutModal` ("Over dit project") with the full project manifesto + tech + AGPL-3.0 license (sticky wordmark header, scrolling body); extracted the wordmark into a shared `ui/Wordmark.tsx` used by both the sidebar header and the modal; removed the "Gemaakt door Ruben V.H." line from the sidebar (now lives in the modal) and pointed the footer "AGPL-3.0" link at the modal. Added a real `LICENSE` file (AGPL-3.0) + `package.json` license field, and dropped the unused `@react-pdf/renderer` dependency. Fixed the gear-menu popover clipping (portaled out of the `.mac-vibrant` backdrop-filter), centered the sidebar wordmark, and made "reken" use `--text-main` (white in dark).

**2026-06-02** — Apple-HIG retheme: swapped the dark/light color tokens to Apple's exact system colors (systemBlue primary `#0A84FF`/`#007AFF`, neutral systemGray surface ramp, system-color domain accents; purple kept as the *meetkunde* dot), colorblind theme left untouched. Migrated icons from lucide-react to **Phosphor** (`@phosphor-icons/react`) across 10 files and added SF-Symbols-style weight-on-interaction — IconButton + the sidebar theme button thicken their glyph (`bold`) on hover/focus, and `active`/`primary` icons sit bold at rest — via an app-wide `IconContext` default in main.tsx.

**2026-06-02** — Added a far-right search bar to the MassAdd ("Oefeningen toevoegen") modal (sidebar-style, live-filters cards by label/context/variant, with a "geen oefening gevonden" empty state). Added an icon-only "Alle blokken wissen" button to the TopBar (Trash2, confirm-guarded, spaced left of Delen) backed by a new `clearBlocks` store action that pushes history so Ctrl+Z restores.

**2026-06-02** — Responsive rework for small laptops: decluttered the TopBar (autosave prompt moved to a dedicated second row so it never clips; one **Afdrukken** button with a blad/blad+oplossingen menu instead of two; Delen is now icon-only; Exporteer/Importeer/Presets moved into the sidebar gear). Added `PanelShell` + `useMediaQuery` so below 1800px the Sidebar and Inspector collapse to hover/focus flyouts with a per-side pin (localStorage); A4 stays 1:1 so print is untouched. Theme moved to a cycling icon next to Help (removed the Thema row); sidebar subtitle default → "Ruben V.H." (localStorage key bumped v3→v4).

**2026-06-02** — Added a favicon: the Enderklas logo downscaled to a 64×64 `src/assets/favicon.png` (~2 KB, from the 1024² source), linked via `<link rel="icon">` in index.html (Vite hashes/bundles it).

**2026-06-01** — Fixed gray print background (regression): the shadow-clip `.print-scroll` padding wasn't reset in `@media print`, so the inset sheet exposed the gray `--bg-base` (forced to print by `print-color-adjust:exact`). Added `padding:0` to the print `.print-scroll` rule + `background:white` on `.print-root`. Also fixed oppervlakte Hulptabel header/body right-edge drift (header `colW` now matches the collapsed 2-cell column width).

**2026-06-01** — Oppervlakte herleidingen: (1) fixed a generator bug where compounds emitted a square **and** its are-alias in the same place (`47 m² 0 ca`) — standard formats now dedupe to one unit per factor (`gridUnits`, squares win). (2) New oppervlakte-only "Soorten oefeningen" **Vierkant → are** and **Are → vierkant** (ca=m², a=dam², ha=hm²) with an **Are-omzetting** toggle (Enkelvoudig 1-op-1 / Are-stelsel samengesteld) shown when an are-type is picked; reuses the max-getal sliders. New constraint `areMode` (default `samengesteld`); format union + `herleidingenDefaults` extended. Integer-exact (isExact guard) and recompute unchanged.

**2026-06-01** — Herleidingen **Hulptabel** overhaul in [HerleidingenViewer](src/components/viewer/HerleidingenViewer.tsx): (1) prompt column got a fixed shared width (≈8.4px/char) so unit cells no longer staircase; (2) `Antwoord = Ingevuld` answer lines shrink when ≥3 parts (volledig) so they stay on one row; (3) **oppervlakte** now renders **2 sub-cells per unit column** (×100 = 2 digits) with columns grouped by factor — the are-units stack on their squares (ha/hm², a/dam², ca/m²) when both enabled. Cellbreedte slider still controls column width. Absolute extreme (all 10 area units + answer) stays user-managed (fewer units / smaller cellbreedte / Antwoord verborgen).

**2026-06-01** — Default theme is now **light** for first-time users (`loadInitialTheme` fallback `dark`→`light`; persisted choices unaffected). First-run banner is now a Dutch **welcome** message (lucide `Hand` icon, not an emoji) instead of the "Nieuw: …" changelog — same once-per-`RELEASE_VERSION` gating + dismiss; App no longer imports `RELEASE_SUMMARY`. Also recorded a decision NOT to horizontally center the MassAdd/curriculum example previews (comment at `ExercisePreview` `scaleWrap` + memory): centering collapses width:100% viewers (number line, thermometer) — left-aligned is intended.

**2026-06-01** — Pre-tester fixes. **A4 sheet**: `height` `max-content`→`auto` so the white card grows past one page (real `<table>` child made `max-content` unreliable); `.print-scroll` got `padding: 8px 48px 48px` so the sheet's 48px-blur drop-shadow isn't clipped (overflowY:auto forces overflow-x to auto). **MassAdd/curriculum previews** ([ExercisePreview](src/components/shared/ExercisePreview.tsx)) now scale by **width AND height** (uniform) so tall examples — thermometer, teruggeven diagram — shrink to fully fit the card instead of clipping. **Puntoefeningen** (a + . = c) hide the Kort/Lang/Stappen scaffolding control (Inspector) and always render `inline-short` ([MathBlockRenderer](src/components/viewer/MathBlockRenderer.tsx)) across + − × ÷ and natuurlijk/decimaal/rationaal. tsc + lint + build green.

**2026-06-01** — Header/Inspector/Help polish batch. **Autosave prompt** now a centered rounded-rectangle callout in the middle of the top bar (no fill, accent border + `RotateCcw` restore icon; Ja/Nee are outline-only purple buttons) — [TopBar](src/components/layout/TopBar.tsx) props `autosaveTitle`/`onAcceptAutosave`/`onDeclineAutosave`. **Koptekst stijl** gained **"Onderstreept"** (order Geen · Onderstreept · Kader) drawing one line under the whole header (`headerStyle: 'geen'|'onderstreept'|'kader'`). **Center title** now flanks a narrow field set (e.g. Naam left / Datum right of the centred title) when it fits one A4 line, else falls back to the stacked layout ([App.tsx](src/App.tsx) `renderFields` gained a subset arg). **Footer defaults**: school/klas/leerkracht off by default (only paginanummer on). **Help modal**: teacher "Bugs of suggesties?" now links the contactformulier (forms.gle) instead of X; parents gained a "Steun deze tool" coffee/❤ link to buymeacoffee. **Inspector** right panel top padding dropped to 0 so its first card top-aligns with the TopBar/sidebar tops.

**2026-06-01** — Fixed UI-overhaul regression: all modals were trapped inside the `.mac-vibrant` TopBar/Sidebar because `backdrop-filter` makes an element the containing block for `position:fixed` descendants (only worked in colorblind, where vibrancy is off). New [ModalPortal](src/components/ui/ModalPortal.tsx) (`createPortal` → `document.body`) now wraps MassAdd / Preset / Help / Curriculum / Basisinstellingen overlays so they center over the viewport again. Also lifted the "Vorige werkbundel gevonden" autosave banner into the pinned top-bar region (no longer scrolls with the sheet) and removed the "Nieuw: nieuw:" doubling in `RELEASE_SUMMARY`. tsc + lint + build green.

**2026-06-01** — Herleidingen wide-row overflow fix + clearer format labels. The long (compound) conversion side now **wraps** to a second row instead of running off the A4 page. New per-block **Uitlijning** toggle in the *Geavanceerd* accordion (constraint `herleidingLayout`): `uitlijnen` (default — given on the left, shared "=" column) or `compact` (single-part side on the left so the long compound always gets full width; flips `samengesteld→enkel` to `___ mm = 139 km 3 hm …`). "Soorten oefeningen" reverted from illustrative glyph pills to stacked, equal-width **text-label** buttons. tsc + lint + build green.

**2026-06-01** — Herleidingen UX pass + layout fixes. **Layout**: center-column scrollbar now starts at the viewer (TopBar lifted out of the scroll container into a `.print-scroll` wrapper; [App.tsx](src/App.tsx)/[appStyles](src/styles/appStyles.ts)/print CSS); gear popover no longer clipped (sidebar dropped `overflow:hidden`); Oefeningen (engine) card uncapped (no 520px inner scroll) + inspector/sidebar top padding trimmed to align the column tops. **Herleidingen config**: enkelvoudig max is a smooth linear slider (10–1000); format pills show illustrative blank patterns ("__ cm", "5 __", …). **Viewer**: blocks with wide exercises auto-drop to one full-width column; the Hulptabel is centered with an optional left **prompt** column + a right **= ___ answer** column (blanco/ingevuld/verborgen) + cell width/height sliders (controls in Differentiatie; constraints `tablePrompt`/`tableAnswer`/`tableCellW`/`tableCellH`). **Plaatswaarde** waarde/plaats prompts right-aligned (fixed width) so arrows + answer lines align. Also fixed a **TourOverlay** render loop. tsc + lint + build green.

**2026-06-01** — Herleidingen refinements + **Oppervlakte**. **Two breakpoint max sliders** ([HerleidingenConfig](src/components/configurator/plugins/HerleidingenConfig.tsx)): `maxEnkel` (stops 10/100/1000, def 100) for the single formats, `maxSamengesteld` (10…1 000 000, def 1000) for the compound formats. **Compound depth** toggle `compoundMode` 2 eenheden / Volledig (full contiguous decomposition, e.g. `194 mm = __ dm __ cm __ mm`). **Oppervlakte** measure added (squared ladder km²…mm² + are-units ha/a/ca as equal-factor aliases; strict descending `pickPair`; safe-integer guard so huge km² products never float-break — validated 144 000 batches, 0 errors). **Hulptabel** scaffolding control moved from the Oefeningen card to the **Differentiatie** card (Inspector). "Eenheden" → "Maateenheden". **Inline edit on the A4 viewer**: click a given number to edit it, click a unit for a **dropdown** of valid ladder units (no typos); `recomputeHerleiding` restores the conversion and `patchExercise` persists. tsc + lint + build green.

**2026-06-01** — Tree reorg + new **Herleidingen** (metric unit-conversion) family. **Reorg** ([appstructure.ts](src/config/appstructure.ts), Getallenkennis): Getalbegrip type order (Plaatswaarde above Splitsen, Vergelijken above Ordenen); Even/oneven moved out of Getalbegrip into **Deelbaarheid**; **Breuken** moved up directly under Getalbegrip; **Romeinse cijfers** moved to the bottom; **Gehele getallen** placeholder removed. **Herleidingen** (lengte/inhoud/massa; oppervlakte stays placeholder) — new [herleidingenGenerator.ts](src/services/herleidingen/herleidingenGenerator.ts) (integer-exact metric ladders via `ladderFor`; 4 formats: enkel-getal `1 kg = __ g`, enkel-eenheid `2 l = 200 __`, samengesteld→enkel `3 m 6 cm = __ cm`, enkel→samengesteld `540 cm = __ m __ dm`; dedupe loop; conversions validated over 2000 batches), [HerleidingenViewer.tsx](src/components/viewer/HerleidingenViewer.tsx) (aligned `=` column, blank/solution lines, write-units option, optional salmon conversion-table scaffold with/without headers — N rows), [HerleidingenConfig.tsx](src/components/configurator/plugins/HerleidingenConfig.tsx) (unit pills, max slider def 9, format pills, write-units toggle, scaffolding segmented). Wired types/registry/exerciseUI; the 3 leaves set measure+units. tsc + lint green.

**2026-06-01** — macOS-faithful sweep + comfort themes + onboarding (branch `ui-overhaul`). **Themes**: Apple-system comfort — dark base lifted `#0d0d10`→`#161619`, off-white text `#ececef`; light base `#efeee9`; colorblind unchanged. **Engine card** unified to the standard `S.card`+`S.cardTitle`; Genereer is now a primary `IconButton` (Sparkles). **M1 segmented**: macOS unified `.seg-group`/`.seg-btn` (one bezel + dividers + tinted selection) for the Inspector segments + theme switch; list-row selectors keep the separated `S.radioBtn`. **M3**: subtle bezel on IconButton. **M4/M4b sidebar**: domains render as source-list section headers — full-width accent-tinted bands (color-mix 12%) with dot + label in the domain accent (replaced the colored left rail). **M5 vibrancy**: `.mac-vibrant` frosted-translucent TopBar + sidebar (backdrop-blur; colorblind stays opaque; Inspector opaque). **P3**: sidebar subtitle now the credit "Gemaakt door Ruben Van Handenhove"; footer dupe removed. **P4**: "Geavanceerd" accordion → a `⚙` gear icon + upward popover in the footer (`BaseSettingsPanel` rewritten). **P5**: new [TourOverlay.tsx](src/components/onboarding/TourOverlay.tsx) — interactive first-run spotlight (add→settings→generate→print→WIP/feedback finale), one-time via `enderklas_tour_seen_v1`, replay from Help's "Rondleiding"; replaces AlphaPopup (removed), folding its WIP warning into the last step. Docs synced (ARCHITECTURE/CLAUDE/UI-GUIDE). tsc + lint green.

**2026-06-01** — UI unification + style handbook (branch `ui-overhaul`, follow-up to the Apple-HIG re-skin). **Canon**: `sharedPluginStyles` gained `maskBtn`/`bridgeBtn`/`groupLabel`; Inspector `S.radioBtn` + sidebar `themeBtn` dropped the raised-thumb for the one canonical selected look (accent-soft tint + accent text + 1px accent ring). **Sweep**: every config plugin (incl. `addition/*`, `multiplication/*`, `Cijfer`, all `Geld*`, `Splitsen`, mask-only configs) moved onto the shared helpers — all local `maskBtnStyle`/`radioBtnStyle`/`bridgeBtnStyle`/`pill`/`toggle`/`tableBtn` deleted; hardcoded `#222226`/`#fff`/`white`/`rgba(155,48,255)`/`--accent-bewerkingen` control colors removed; group titles → `groupLabel`; GeldTeruggeven slider → `Switch`. **Hotfixes**: colorblind `--shadow-1: none` (stop the TopBar doubled-edge), MAB default `boxHeight` 60→70 (registry + fallbacks) so thousands don't clip. **Sidebar**: `+` glyph → centered lucide `Plus` (accent on hover); width reclaimed (no domain marginLeft, subdomain font→`--text-sm`, nav labels ellipsis); logo 56→48; footer name on its own line. **Block rail**: delete moved to the bottom behind a divider (anti-misclick). **Features**: click the opdracht title on the A4 preview to edit inline (`EditableInstruction` → `updateBlockInstruction`); print-with-solutions button now uses a `Key` icon. **Docs**: [UI-GUIDE.md](UI-GUIDE.md) rebuilt as the authoritative handbook (scales, control catalog, the single selected-state rule, two label tiers, "no local control styles", compliance checklist). tsc + lint green.

**2026-06-01** — Apple-HIG UI overhaul (app chrome only; worksheet print untouched). On branch `ui-overhaul`, baseline tag `ui-overhaul-baseline` (dev+main synced to it on origin). **Tokens** ([theme.css](src/assets/theme.css)): added 8pt spacing / radius / type / motion scales to `:root`, and per-theme layered surfaces (`--bg-base/-surface/-surface-2/-hover`), hairline `--separator`, desaturated `--accent` (was neon `#ac29e9`), `--accent-soft/-on`, `--danger`, soft `--shadow-1/2/3` + `--shadow-focus` — for all three themes; old token names kept as aliases. UI font → SF-first `--font-ui`. **index.css**: global `:focus-visible` ring, `prefers-reduced-motion`, `.ui-icon-btn`/`.ui-hover`/`.sidebar-leaf` hover-press helpers, floating scrollbar. **Controls**: `sharedPluginStyles` radio/pill → calm accent-soft segmented thumbs, sentence-case labels; `IconButton` retokened (34px, hover/press); new [Switch.tsx](src/components/ui/Switch.tsx) iOS toggle (used for the 3 Opdrachtinstellingen booleans). **Panels** re-skinned via their `S` objects (appStyles block-selection = accent ring not dashed; sidebar removed inline hover hacks → `.sidebar-leaf`, segmented theme switch; TopBar menu hover; Inspector cards = surface+shadow, segmented `btnGroup`, pill generate button). Killed tiny-UPPERCASE micro-labels — hierarchy now weight+size. tsc+lint green.

**2026-06-01** — Print fix + footer + natural/decimal selector. **Print**: added `print-color-adjust: exact` in [index.css](src/index.css) `@media print` so background-color visuals print (lijnstuk line, salmon rooster headers, fills, tints); also redrew the Breuk-van-een-lijnstuk segment line with borders instead of `background-color` so it prints regardless of the dialog's "Background graphics" toggle. **Footer** ([sidebar.tsx](src/components/layout/sidebar.tsx)): "Gemaakt door Ruben Van Handenhove"; removed the X link, added a Google-Form feedback link (lucide `MessageSquare`); coffee heart kept. **Natural/decimal selector** (Splitsen-Rooster style "Decimalen: Geen/1/2/3") added to **vergelijken** (getallen + kiezen) and **plaatswaarde** (waarde/plaats/tabel): generators build decimal numbers + decimal masks (t/h/d) via `getMaskPlaces`/`digitAtPlace`; PlaatswaardeViewer renders the comma + decimal places/labels (tienden/honderdsten). Even/oneven left natural-only (undefined for decimals).

**2026-05-31** — Review tweaks to the new families (build + lint green). **Romeinse**: removed the `subtractief` toggle — notation is now always subtractive (IV, curriculum standard); generator/config/defaults updated. **Scaffolding leak fix**: the fraction `subType`-keyed Differentiatie blocks in [Inspector.tsx](src/components/configurator/Inspector.tsx) now only fire for `typeId === 'breuken'` (scoped `subType` to breuken), so Romeinse herkennen no longer inherits the fraction scaffolding list. **Afronden** reworked: `numberType` natural/decimal (new **afronden-dec** leaf, real, rooster+simpel); decimal-safe `roundTo` + `targetsFor` (T/H/D/TD vs E/t/h); the **rooster** is now one rooster *per exercise* with a configurable "Aantal getallen per rooster" (`roosterSize`) — "aantal oefeningen" = how many roosters (laid out 2-up); decimal gets a cijfers-na-komma selector; simpel `≈`/answer lines aligned. **Sidebar**: opening a subdomain now auto-expands all its accordions at once (`openTypes` Set replaces single `openType`); individual collapse + search still work.

**2026-05-31** — Big batch: mask-menu styling fix + UI-GUIDE.md + 5 new exercise families + Geld move (build + lint green; one bracketed commit per family). **[config]** vermenigvuldigen/delen "Specifieke getalopbouw" mask buttons now use the canonical 28×28 / `var(--bg-input)` / 1px-border style + `styles.section` (was a dark hardcoded `#222226` block). **[docs]** new **[UI-GUIDE.md](UI-GUIDE.md)** documents every theme.css token (dark/light/colorblind), the canonical config/Inspector style helpers, the mask-button canon, and the sanctioned worksheet-ink hex colors; linked from CLAUDE.md ("use tokens, never hardcode bg/text/border/accent hex"). **New families** (each: typeId + generator + viewer + config + registry/UI/appstructure, reusing `getMaskPlaces`/`PLACE_VALUES`, `FragmentableGrid`, `formatMathNumber`): **plaatswaarde** (waarde/plaats/tabel), **even-oneven** (rooster kleuren / cirkels groeperen), **vergelijken** (twee getallen <,>,= / grootste-kleinste kiezen), **afronden** (rooster van afrond-doelen / eenvoudig ≈; afronden-nat now real, afronden-dec still placeholder), **romeinse-cijfers** (herkennen → getal / schrijven → Romeins, Niveau N1–N4 + subtractief toggle). **[geld]** flattened the "Rekenen met geld" accordion into direct Korting/Intrest/Winst placeholder leaves. Also: dropped the redundant subType selector from PlaatswaardeConfig (sidebar picks the view); moved "Veelvouden aanvullen" from Deelbaarheid to the "Veelvouden en delers" subdomain.

**2026-05-31** — Sidebar footer/help tweaks ([sidebar.tsx](src/components/layout/sidebar.tsx)). Moved the Help (?) button out of the footer to the right of the "Zoek oefening…" box (`searchWrap` now a flex row). Footer: dropped "— gratis beschikbaar", put "Gemaakt door Ruben V.H." + 𝕏 + ☕ icons on one row with "Code onder AGPL-3.0." below; set the real Buy-Me-A-Coffee link (`buymeacoffee.com/raregoudvis`, was REPLACE_ME placeholder).

**2026-05-31** — Teacher-feedback batch across breuken / geld / vermenigvuldigen / splitsen (build green). **Breuken kleuren**: "Vorm" is now 3 multi-select toggles (Rechthoek/Vierkant/Cirkel, mixed per exercise via new `shapes[]`); new `'square'` `FractionShape` renders a square in vertical strips ([FractionShapeSVG.tsx](src/components/viewer/FractionShapeSVG.tsx)); single-shape selection unlocks a **Vaste grootte** toggle with cm sizing under a new fractions **Geavanceerd** section (`staticSize`/`staticW`/`staticH`/`staticSide`/`staticDiam`). **Breuk van een hoeveelheid — concreet**: unified task to "Verdeel en kleur [breuk]" across all three scaffolds (dropped "van deze hoeveelheid"); new **Groepering** setting (`groupingMode`: standaard / gebalanceerd / per-deel) via `groupRows()`. **Schematisch**: draw-box width/height now adjustable in cm (`drawBoxW`/`drawBoxH`). **Abstract**: N1/N2/N3 and Berekenings-/Structuurlijnen/Blanco now show example hints under each button. **Veelhoek**: `showGrid` toggle — off = outline + colored region only, 1cm cells. **Vermenigvuldigen**: "Zonder ×1" (`excludeOne`) toggle as last engine setting, filtered in mathEngine. **Geld teruggeven**: italic "Teken dit bedrag." in the draw box, "Berekening:" in the lege-ruimte box. **Verliefde harten**: getalopbouw labels → "Getal bovenaan" / "Ingevuld getal".

**2026-05-31** — Roadmap placeholders added to `APP_STRUCTURE` (greyed, non-clickable, no generators yet) marking the next exercise types to build, based on a wiskunde-leerplan coverage analysis (L1–L6, G/`+` doelen). **Getallenkennis** (Getalbegrip): Vergelijken (<,>,=), Verbanden (breuk·decimaal·procent), Even en oneven, Plaatswaarde benoemen. **Bewerkingen**: new placeholder subdomains Hoofdrekenen (handig rekenen) [×/:10·100·1000, compenseren/splitsen, rekenvolgorde en haakjes, kettingsommen], Schattend rekenen, Controleren (negenproef). **Meten**: Tijdsduur, Kalender, Lengte en oppervlakte (liniaal/omtrek/oppervlakte), Massa (weegschaal), Maateenheden (passende maateenheid kiezen), Herleidingen (lengte/oppervlakte/inhoud/massa). New **Vraagstukken** domain (placeholder) + `--accent-vraagstukken` in all 3 themes (theme.css). Meetkunde deferred. Coverage/projection tooling lives in a local scratch dir outside the repo (not committed): generatable ceiling ≈ 60% of the leerplan, roadmap lifts coverage 30%→45% of total (50%→75% of generatable).

**2026-05-31** — Docs + release for the multi-round feature work. Updated ARCHITECTURE.md (new state slices baseSettings/curriculum/draftBlocks, history/lock-gate rules, autosave incl. curriculum, persistence v2 + lz-string + CurriculumLock, new §13 "teacher-workflow layer", refreshed file map + registry rows), CLAUDE.md (directory tree + state slices + persistence/teacher note), README (features + types table). Bumped RELEASE_VERSION → v0.5 + RELEASE_SUMMARY and package.json → 0.5.0. Pushed `dev` + `main` for external testers.

**2026-05-31** — Round-5 splitsen/temperatuur fixes (build + lint green, app boots). **Splitsen mask bug**: `generateTotal`/`generateGiven` were double-counting decimal places (PLACE_VALUES already holds t/h/d) → Rooster now honors a t-only mask. **Positie masks + decimals**: splitsbenen/plaatswaarden/positietabel get a "Specifieke getalopbouw" mask under Maximum getal; the generator now *builds* the number from that mask (numFromMask), so "only TE,t" works; positietabel gained decimals (numberToDutchWords extended with "komma" for decimal digits) and its tables are right-aligned (word fills the left, no overlap). **Soorten** pills now lay out 2×2 (Benen row / Getal row). **Manual edit**: splitsen Geavanceerd section lists a textbox per generated number; typing one re-derives that exercise (pairs / place breakdown / words) via `recomputeSplitsenExercise` + `patchExercise`. **Temperatuur**: scale capped at 25 (glass continues a bit past, unlabelled) and negatives floor at −15; "Per rij" hidden for the verschil type (one exercise per row).

**2026-05-31** — Round-4 feedback fixes (build + lint green, app boots). **Preview**: reverted the auto-fit to width-only scaling + clip (no more distorted/compressed examples). **Font weight**: swept all viewer *content* to normal weight (solutions, answers, totals, fraction digits, etc.); only the opdracht title + structural table headers stay bold. **Config restyle**: added `pill` + `onOffRow/onOffBtn` helpers to sharedPluginStyles; Splitsen "Soorten"/"Notatie" are now themed pills, Getallenas/Ordening fraction toggles are label + Aan/Uit. **Splitsen**: "Splitsen (basis)" → **Rooster**; new **Decimalen (0–3)** setting + decimal place-value masks (t/h/d) for Rooster + splitsbenen + plaatswaarden (positietabel + verliefde harten stay integer); generator does scaled-int decimal decomposition, viewer formats via formatMathNumber. **Temperatuur**: removed the duplicate Variant toggle from the engine (variant = sidebar leaf); redesigned the thermometer SVG (glass gradient, bulb, minor+major ticks); new **Verschil** type — two thermometers with per-thermometer given-info (Gekleurd/Getal/Beide) + a "verschil = ___" answer (`|c1−c2|`).

**2026-05-30** — Round-3 polish + decimal/fraction depth (build + lint green, app boots; testing build for external feedback). **Preview**: `ExercisePreview` now measures + scales AND centers the example in its white box (translate offset; inner sized to max-content). **Sidebar/engine dedup**: reverted Splitsen to separate sidebar leaves (accordion, no wiskundig) and removed the layout picker from `SplitsenConfig`; removed the duplicate "Lay-out" toggle from `DeelbaarheidConfig`; nested Klok activity (Lezen/Tekenen/Omzetten) under Analoge/Digitale klok in the sidebar and dropped the Activiteit toggle from `ClockConfig`. Catalog disambiguates multi-parent variant labels (e.g. "Analoge klok · Lezen"). **Ordening decimals**: 1/2/3 decimal places (Decimalen control + mask shows t/h/d). **Getallenas**: decimal gets fine step presets (down to 0,001) + a validated custom-step text input; rational gets Gemengde getallen + Gelijknamige (niet vereenvoudigen) toggles; geheel got a minimum-bound slider and fixed descending/negative runs (clamped start math). **Base parity**: `baseDecimalPlaces` + `baseUnitFractionsOnly`/`baseAllowMixed` in Basisinstellingen, wired via `baseApply` (ordenenDefaults declares the keys). **Bug fix**: `digitAtPlace` now scales to integers so 3-decimal place masks extract digits without float drift.

**2026-05-30** — Big batch: preview/header fixes + Splitsen/Ordenen/Getallenas upgrades (build + lint green; needs in-app verification). **Shared**: new `VerticalFraction` component (extracted from MathBlockRenderer/FractionExerciseItem, repointed both); generic `patchExercise(blockId, field, exId, patch)` store action; `numberMatchesMask` + `digitAtPlace` in mathEngine. **A — fixes**: `ExercisePreview` now measures content + scales to fit a fixed box (ResizeObserver), so mass-add cards show the WHOLE example; mass-add grid → 2 columns (minmax 460) + taller preview. Sheet header (center title): Score box moved onto the Naam/Klas row (no longer floats below). **B — Splitsen**: layout selection moved out of the sidebar (single "Splitsen" leaf) into an inspector **dropdown** (wiskundig dropped); positietabel words right-aligned so tables align; splitsbenen gained a 30-vs-3T notation + a 4-combo (benen/getal × 30/3T) include-checklist that the generator mixes; plaatswaarden notation became letters/uitgebreid checkboxes mixed per exercise (new `SplitsenExercise.notation`). **C — Ordenen**: count up to 8; click-to-edit prompt numbers (re-sorts the answer via patchExercise); "specifieke getalopbouw" place masks for natural+decimal; rational shows vertical fractions with min/max denominator + stambreuken + gemengde-getallen toggles; gehele getallen got a lower-bound slider (−maxGetal…0). **D — Getallenas**: "Beide" arrow direction (mixed per set); decimal/rational/geheel number lines now real (generator precomputes `values: (number|Fraction)[]`, viewer renders an HTML label overlay with vertical fractions + nl-BE/negative formatting).

**2026-05-30** — Inspector/sidebar/help polish (build + lint green). **Geavanceerd** sidebar group ([BaseSettingsPanel.tsx](src/components/layout/BaseSettingsPanel.tsx)) is now a collapsed-by-default accordion. **Opdrachtblok** card ([Inspector.tsx](src/components/configurator/Inspector.tsx)) redesigned to sliders: Instructie (text, placeholder "Los op."), Score (slider 0…aantal×2, clamps when aantal drops), Aantal oefeningen (slider 1–20), Witruimte (renamed Spatiëring); locked mode disables all but Aantal. **Curriculum samenstellen** left list now shows **subdomain sub-headers** (groups by `item.context`, e.g. Geld → Herkennen/Bedrag tekenen/Wissel/Teruggeven) and the modal is wider (1280 / list 360). **HelpModal** rewritten into **Voor ouders** / **Voor leerkrachten** tabs; the teacher tab has a Basis block (original sections) + a Geavanceerd block documenting Basisinstellingen and Curriculum samenstellen.

**2026-05-30** — Round-2 refinements on the three workflow features (build + lint green; needs in-app verification). **Shared `ExercisePreview`** (new `components/shared/`, replaces MassAddPreview): builds a 1-exercise throwaway block, generates directly, renders the Viewer in a fixed-height clip box (`scale(.85)`, width-compensated) so previews are uniform. **Mass-add**: now 1 example/card + a top-level **domain filter bar** (chips from new `catalogDomains()`; `CatalogItem` gained `domainId`/`domainLabel`). **Basisinstellingen** enriched + moved: `BaseSettings` now carries per-place `baseOperand1Mask`/`baseOperand2Mask` + `baseBridges` map (replacing the single `baseBridgePolicy`); `baseApply` writes them cloned+guarded (only when non-empty). New **`BaseSettingsModal`** mirrors the real NaturalSettings UI (max presets, D/H/T/E getalopbouw, GEEN/MAG/MOET bruginstellingen via `getMaskPlaces`/`getBridgePlaces`). **Curriculum samenstellen** rebuilt as a proper authoring modal (`components/curriculum/CurriculumBuilderModal.tsx`): two-pane — left = every type grouped by domain with show/hide + bulk (per-domain + global) + variant select; right = the **real config plugin** (`EXERCISE_UI[typeId].Config`) rendered against an off-sheet **`draftBlocks`** store slice (new; `updateBlockSettings` falls through to it, no history/autosave) + a live example; footer emits the locked share link from the drafts. **Top bar** decluttered: **Delen** dropdown (Blad/Sjabloon) + **⋯ Meer** overflow (Exporteer/Importeer/Presets); removed the loose share/template/lock icons and the derive-from-sheet `handleShareCurriculum`. Both **Basisinstellingen** and **Curriculum samenstellen** now live in a sidebar **Geavanceerd** group under Thema (hidden in locked parent mode).

**2026-05-30** — Three workflow features shipped (build + lint green; needs in-app click + print verification). **(1) Global base settings** — new `src/config/baseSettings.ts` (`BaseSettings` {baseMaxGetal, baseNumberType, baseBridgePolicy} + `baseApply` that maps the semantic base onto each type's own keys via `'key' in defaults` guards: maxGetal/maxRange/maxNumber, numberType, bridges E/T/H/D; breuken deliberately excluded). New collapsible `BaseSettingsPanel` at the bottom of the sidebar. Snapshotted into each new block in `addBlockFromType` (order: registry defaults → base → leaf override), so changing the base never retro-affects existing blocks. Persisted in `WorksheetFile`/autosave/presets/share. **(2) Mass-add modal** — new `src/config/exerciseCatalog.ts` (walks APP_STRUCTURE, drops placeholders/non-registry, groups leaves by typeId into variant rows) + `MassAddModal`/`MassAddPreview` (live example per type via direct `REGISTRY.generate` → `EXERCISE_UI.Viewer`, no store writes; per-item error boundary + IntersectionObserver lazy render + re-roll; variant toggles), opened from a new "Toevoegen" TopBar button; "Alles toevoegen" batch-adds then generates. **(3) Locked curriculum templates** — `WorksheetFile` v2 gains optional `curriculum: {locked, allowedTypes[{typeId,label,lockedConstraints}]}`; store `curriculum` state enforced at the single choke point (`updateBlockSettings` allow-lists only numberOfExercises+pageBreakBefore when locked; `updateBlockInstruction`/`updateBlockLayout` frozen); sidebar swaps to a flat whitelist palette (hides search + base panel), Inspector shows a lock banner + hides config/differentiation/advanced (keeps count + Genereer), new "Deel curriculum" TopBar action derives the whitelist from current blocks and shares a template link; App.tsx adds a locked-load confirm. Lock survives refresh (autosave carries curriculum).

**2026-05-30** — Five new exercise families (basic versions, refine later), all via the registry contract. **#4 ordenen** (`ordenen`, 4 numberType leaves nat/dec/rat/geh): order 3–5 numbers with `<`/`>`/both. **#2 deelbaarheid** (`deelbaarheid`): tick-table (rows × selected divisors) + veelvouden fill-row. **#3 splitsen place-value** (extends `splitsen`, now an accordion banner): new layouts `positie-tabel` (Dutch number-word → D…E digit grid, via new `numberToDutchWords` helper ≤1e6), `positie-benen` (legs → H/T/E, toggle which side blank), `positie-math` (letters `7H+9T+2E` / expanded `300+70+8`, decompose/compose), place values up to 1e9. **#6 getallenas** (`getallenas`, natural only; dec/rat/geh stay placeholders): SVG number line with arrow-direction + easy/hard blank modes, step/max/ticks knobs. **#8 temperatuur** (new "Temperatuur" subdomain under Meten en metend rekenen): SVG thermometer, variants `kleuren` (colour to a temp) / `aflezen` (read a coloured meter), negatives toggle (−20…30). Each = generator + viewer (FragmentableGrid) + config + registry/UI/appstructure rows. Build + lint green; needs in-app + print verification.

**2026-05-30** — Print footer pinned to page bottom + sticky TopBar. Footer (`<tfoot>`) was floating under the last block on a short last page; added a `height:100%` chain (html→body→#root→.print-root→.print-main→.print-area-shell→`.print-area` table) in the `@media print` block so the table fills the page, the tbody cell absorbs the slack, and `<tfoot>` sinks to the page bottom. TopBar wrapper made `position:sticky; top:0` (it's a direct child of the `overflowY:auto` center column) so the crucial toolbar stays visible while scrolling the preview. Build + lint green; needs a print check (watch for a stray blank page on near-exact-fit sheets).

**2026-05-30** — Shipped **v0.4**. Bumped `RELEASE_VERSION` → v0.4 (+ Dutch banner summary on the print improvements) and package.json → 0.4.0. Rebuilt README.md (kept the Dutch teacher intro, added an English "Architecture — how it fits together" section: 3-panel data flow, the exercise registry, store, generator contract, print system, persistence). Refreshed the now-accurate print docs in ARCHITECTURE.md §9 (+§8/§11/§12) and CLAUDE.md to the real-`<table>` repeating header/footer + `@page margin:0` + FragmentableGrid system. Committed the whole working tree (registry restructure + print rewrite + docs), fast-forwarded `main` to `dev`, tagged `v0.4`, pushed main/dev/tag to origin.

**2026-05-30** — Exercises now flow across page breaks + header field alignment. Multi-item viewers each put all items in one `display:grid`, and Chrome does not fragment a grid container across printed pages — so a block taller than the remaining space jumped wholesale to the next page (reported for "Breuk van een lijnstuk" and "analoge klok"). Added a shared `FragmentableGrid` (src/components/viewer/FragmentableGrid.tsx): a block container holding per-row grids, each row `break-inside:avoid` (`.print-row`) so rows never split but the block breaks between them — the same row-chunking pattern CijferViewer already used successfully. Converted all grid-based viewers (Clock, Fraction, MathBlockRenderer, Geld, GeldTekenen, GeldWissel, Mab, Splitsen basic+mathematic), preserving each one's columns/gaps/justify/align; flex-column/flex-wrap viewers (Cijfer, GeldTeruggeven, Splitsen verliefde-harten) already fragment and were left alone. Header: `renderFields` gained an `align` param so the fill-in fields sit opposite the title — title-left now right-aligns Naam/Klas/Nr/Datum (title-right/center unchanged). Build + lint green; needs a Chrome/Edge print check.

**2026-05-30** — Print footer/header now repeat on EVERY page (real `<table>` markup). The prior div-based `display:table-header-group`/`table-footer-group` worked in Firefox but Chrome/Edge (Blink) only repeats `<thead>`/`<tfoot>` across printed pages for **real** table elements — so the teacher's PDF showed the footer only on the last page and the repeat-header strip only on page 1. Rewrote the print card in App.tsx as a shell `<div>` (keeps the screen card look + `a4Sheet`, holds `a4Ref`, and is the positioned container for the page-break indicators, which moved out of the table) wrapping a real `<table class="print-area">` with `<thead>` (spacer + repeating name strip), `<tbody><tr><td class="print-body-cell">` (header + blocks, 16mm side padding on the cell), and `<tfoot>` (school left / vrije tekst right). Screen CSS flattens the table to block flow (display:block on table/tbody/tr/td, thead/tfoot hidden) so the preview is unchanged; print CSS uses native table groups and `.print-area-shell { display:block !important }` to override a4Sheet's inline flex (flex must never reach the `<table>` or Chrome drops `display:table` and stops repeating). Real spacer `<div>` replaces the `::before`; `.print-body-inner` dropped (the `<td>` carries padding). Build + lint green; needs a final Chrome/Edge print check.

**2026-05-30** — Robust print rewrite: real margins + repeating non-overlapping footer. Teacher's printed PDF showed `@page` margins ignored (content at paper edge), the `position:fixed` footer overlapping/cutting exercise bottoms, and a page number where vrije tekst should be — all because the layout depended on `@page` margins that the print dialog ("Margins: None/Minimum") silently zeroes. Switched to the CSS table running-header/footer technique: `.print-area` becomes `display:table` in print with `.print-thead` (table-header-group, top-margin spacer), `.print-body`/`.print-body-inner` (table-row-group, 16mm side padding), and `.print-tfoot` (table-footer-group, school-left / vrije-tekst-right). Browsers repeat header/footer groups on every page AND reserve their height, so margins and footer survive any dialog setting and content never overlaps. `@page` margin set to 0; usePrint.ts still blanks the browser margin boxes. Body wrappers are `display:contents` on screen so the preview is unchanged. Voettekst menu: dropped Paginanummering (Chrome can't count pages), moved Vrije tekst to the right zone. New "Koptekst op elke pagina herhalen" toggle (`HeaderData.repeatHeader`) extracts the name fields into a `renderFields()` helper and repeats them in `.print-thead` on every page (page-1 body copy hidden to avoid duplication). Build + lint green; needs a manual Chrome/Edge print check.

**2026-05-30** — Fix print footer + page-width regression from the print overhaul. The footer had been moved into `@page` margin boxes (`@bottom-center { content: counter(page) }`), but Chromium ignores margin-box `content`, so Chrome/Edge printed blank page bottoms. usePrint.ts (blank-boxes-only) and index.css (16mm sides, `.print-footer-bar` fixed rule, `.print-area` bottom padding) were already corrected in a prior pass; restored the missing HTML footer in App.tsx — re-added the `footerData` store selector and the print-only `.print-footer-bar` div (school | klas | leerkracht left, vrije tekst right, `space-between`). No live page number (Chrome can't count pages from HTML/CSS without a paginator). Build + lint green.

**2026-05-30** — Share-link compression + print/layout overhaul. Share: links now LZ-compressed via lz-string (`compressToEncodedURIComponent`, ~6–8× smaller) with cap raised 6000→30000; old base64 links intentionally dropped (alpha). Print/layout: new global "Ruimte tussen oefenreeksen" slider (`DocSettings.blockSpacing`); preview widened 800→920px; print side margins 18→14mm, top 15→18mm for less-cramped continuation pages; sets may now split across pages (`break-inside: avoid-page` removed) while opdracht line stays glued to its first row (`.print-opdracht break-after:avoid`) and single exercises never split; per-block "begin op nieuwe pagina" hover toggle (`MathBlock.pageBreakBefore` → `.page-break-before { break-before: page }`) with on-screen "↡ nieuwe pagina" marker; real per-page footer (centered `counter(page)` + school info) injected as `@page` margin boxes by usePrint.ts, replacing the hardcoded "Pagina 1" HTML footer. Build + lint green.

**2026-05-30** — Lint cleanup: cleared all 41 pre-existing eslint errors, no behaviour change. mathEngine dead initializers (`let d1=1,…`) → typed uninit decls; `op1: any` → `number | Fraction`, `op2` let→const; `value: any` → `unknown` in 6 config plugins; `isFraction` param `any`→`unknown`; `MathBlock.constraints` kept `any` with a documented eslint-disable; AlphaPopup lazy `useState` init (dropped the effect); eslint-disable for the boot-time autosave `setState`; eslint config override adds `argsIgnorePattern`/`varsIgnorePattern: '^_'` for intentionally-unused args. Build + lint both green.

**2026-05-30** — Central registry refactor: replaced the scattered `typeId` if-else wiring (dispatch, Inspector config mount, App viewer routing, `addBlockFromType` defaults) with a single exact-key registry — `config/exerciseRegistry.ts` (pure data: generator/field/defaultConstraints/defaultCount) + `config/exerciseUI.tsx` (Viewer/Config). Store gained a generic `setExercises(id, field, data)` action replacing the 9 per-type setters; `generateDispatch` is now a registry lookup. Added per-family constraint interfaces to types.ts. Normalized viewers to a uniform `{block, showSolutions}` (MabViewer derives mode from typeId; new ClockViewer/FractionViewer wrappers hold the grid logic moved out of App.tsx). Build + lint green. Adding a type is now: generator + viewer + config + one row in each registry file.

**2026-05-30** — Added ARCHITECTURE.md (full system map: data flow, add-a-type contract, generator contract, per-typeId registry table, print/persistence) and brought the stale CLAUDE.md back in sync (removed the dead react-pdf/WorksheetPDF section, updated dir tree + types table + add-type steps for MAB and Geld Wissel/Teruggeven).

**2026-05-27** — Print + Geld polish. Extended `@page` rule with empty `@top-*`/`@bottom-*` margin-box content so Chrome/Edge stop injecting URL/date headers and page-of-Y footers (Firefox still ignores). Tagged per-exercise wrappers in every viewer (Geld/GeldTekenen/GeldWissel/GeldTeruggeven/Mab/Splitsen/Clock + FractionExerciseItem wrapper in App.tsx) with `.print-exercise` so cells never split mid-block; `.print-block` got `break-inside: avoid-page` as soft hint. Bedrag tekenen `Voorbeelden tonen` bar shrunk (Bill 42×24, EuroCoin 26, CentCoin 22, label 9px) so all 13 denominations fit on a single line. Dropped `Hoeveelheid` (D/H/T/E) toggle from Geld herkennen — removed config section, viewer read sites, and `showPlaceValues`/`placeValues` defaults. Geld herkennen denominations defaults shrunk (Bill 56×32, EuroCoin 36, CentCoin 30 via new `width/height`/`size` props on the shared SVG components). New `Geldlayout: 'samen' | 'gescheiden'` constraint + Differentiatie toggle (herkennen only) — `gescheiden` splits bills+euro-coins onto row 1 and cent-coins onto row 2 separated by 10px gap.

**2026-05-27** — MAB v3 polish: symbolic tens rotated to horizontal bars stacked vertically (matches MAB modes, fixes wide-T-column bug). Units render in a deterministic column-first 2-row "domino" pattern (1, 2, 3, 4, 5…) for subitizing — applies to both symbolic dots and Dienes cubes. `MabStyle` renamed: `symbolic` | `mab-bw` (old `realistic`) | `mab-color` (new — units yellow, tens green, hundreds red, thousands blue; black strokes throughout). Back-compat at read sites so old saves with `'realistic'` resolve to `'mab-bw'`. Hundreds-cell bumped to 7px (flat 70×70) so the hundreds glyph reads as the biggest in MAB modes; thousands cell matches.

**2026-05-27** — MAB v2 + tekenen + sidebar branding. Scaffolding model rebuilt as `scaffolding: 'positietabel' | 'kader' | 'geen'` (was `showBox: boolean`); control moved out of MabConfig into the Differentiatie card; backwards-compat fallback at read sites. Geavanceerd accordion hoisted from MabConfig into the shared Inspector accordion (matches cijferen/geld pattern); contains Oefeningen per rij + Tekenvak hoogte + Antwoordlijn hoogte. `boxHeight` default 100→60 (slider min 40). `Tussennullen` toggle + `allowInternalZero` constraint + `hasInternalZero` helper removed — mask now sole structural control. Bug fix: `Tot N` and mask buttons clear `mabExercises` so the preview shows the empty-state prompt instead of stale out-of-range numbers. New `mab-tekenen` (Getallen tekenen) exercise: reverse of herkennen — number printed on the answer line, student draws blocks; shares generator, config, and viewer with herkennen via a `mode` prop. `Geen` scaffolding only offered for herkennen (tekenen always keeps the drawing target). Realistic glyphs: `CELL` 4→6 (60×60 hundreds flat), tens rotated to horizontal bars stacked vertically so all H/T/E columns share fixed width. Sidebar header reworked: logo left, editable site title + subtitle right (subtitle placeholder `using Basisonderwijs Vlaanderen`), both persisted to localStorage keys `enderklas_site_title_v1` / `enderklas_site_subtitle_v1`.

**2026-05-27** — MAB box redesign: drawing now splits into left-to-right H/T/E columns inside one optional box with H/T/E header labels + dividers; single always-shown answer line below. Replaced `scaffolding`/`boxBorderColor`/`useSpecifiek`/`specifiekeGetallen` constraints with `showBox` + `answerHeight`; H/T/E labels bumped to 14px centered. Per-place glyphs split out of `MabBlocksSVG.tsx`; viewer now flex-wraps glyphs per column. MabConfig adds Geavanceerd accordion (Oefeningen per rij + Tekenvak hoogte + Antwoordlijn hoogte sliders) and Met/Zonder kader toggle; Inspector cleared of MAB-specific scaffolding/exercisesPerRow. Block toolbar (lock/duplicate/trash/up/down) relocated to outside-right of the dashed border, vertically stacked. Header gained `fieldOrder` + `fieldWidths` (both optional for back-compat); App.tsx renders fields via `fieldOrder` loop with per-field width, Inspector replaces 4-checkbox grid with reorderable rows (hover ↑/↓ buttons) + per-field length sliders. Centered title position with no title now positions score in flow (`justify-content: flex-end`) so it can't overlap the header answer lines.

**2026-05-27** — Teacher-quality-of-life batch (eight items): (1) Auto-save to `enderklas_autosave_v1` localStorage key, debounced 1.5s via module-level zustand subscribe; boot-time banner offers restore when current sheet is empty. (2) Named preset library (`enderklas_presets_v1`, max 20) with new `PresetModal` (save current + list with Laad/× per row). (3) `document.title` follows `headerData.titel`. (4) Sidebar search filters APP_STRUCTURE tree by label and auto-expands matches. (5) `duplicateBlock` store action + 🗐 icon in block-control overlay (deep-clones constraints + exercises, resets `locked`, switches activeBlock to clone). (6) Donate ❤ link in sidebar footer pointing at Buy Me a Coffee (placeholder URL `buymeacoffee.com/REPLACE_ME` — TODO replace before merge). (7) Release update-banner driven by `RELEASE_VERSION`/`RELEASE_SUMMARY` in new `src/config/version.ts`; dismissed-version cached in `enderklas_release_seen_v1`. (8) Werkbundel via deelbare URL — base64 in `#share=…` hash (not query, so it doesn't leak through server logs); 🔗 Deel button copies link, 6 KB cap with fallback alert, boot-time hash detection with confirm-replace. HelpModal expanded to cover all new features.

**2026-05-27** — Document-wide feature batch: (1) `Genereer alles` in TopBar regenerates all non-locked blocks; new shared `services/generateDispatch.ts` is now single source of truth for typeId→generator routing (Inspector uses it too). (2) Per-block 🔒 lock toggle in block-control overlay + `locked` flag on `MathBlock`; locked blocks skipped by bulk regen. (3) JSON Exporteer/Importeer (`services/persistence.ts`) — versioned schema (v1) covering blocks/header/footer/docSettings; import confirms-and-replaces. (4) Doc-wide `numberBlocks` toggle in Opdrachtinstellingen renders `1.`/`2.`/… prefix on block headers (render-time, doesn't mutate instructionText). (5) Theme system moved into store with three themes (dark/light/colorblind); ☀/☽ sidebar toggle replaced by 3-way Thema card in Inspector; `[data-theme="colorblind"]` block in theme.css uses IBM CB-safe palette. (6) Contact (X-link) + Help icon buttons added to sidebar footer; new `HelpModal` component with sectional Dutch usage guide (cloned from AlphaPopup pattern, follows theme variables).

**2026-05-27** — Added MAB > Getallen herkennen exercise type under Getallenkennis > Getalbegrip. New `MabExercise` type + `mabExercises` field on `MathBlock`. Generator at `services/mab/mabGenerator.ts` supports random-with-mask and manual-number-list paths, optional internal-zero suppression. Viewer renders place-value blocks via new `MabBlocksSVG` component with two styles: 'symbolic' (dot/bar/square) and 'realistic' (Dienes 1×1/1×10 rod/10×10 flat/10×10×10 cube). Scaffolding: answer 'lijn' (dotted line) or 'tabel' (H/T/E columns with row). Config plugin exposes style, maxNumber (10/20/100/1000), mask, manual list, internal-zero toggle, box border color, box height. Sidebar entry added as MAB accordion with 'Getallen herkennen' leaf + 'Getallen tekenen' placeholder.

**2026-05-27** — Print hid worksheet behind mobile-block overlay because print preview narrows the viewport and re-triggers the `max-width: 768px` media query; added `.mobile-block { display: none !important; }` inside `@media print`.

**2026-05-27** — Multiple cijferen mask fixes: Getal 1 multiplicand mask fell back to random when applyMask failed range check (hasMask0 guard added); Getal 2 multiplier/divisor masks ignored decimal keys (t/h) because applyMask was called with dp=0 — fixed to pass dp so fractional multipliers/divisors work; header display for fractional operands now uses dp instead of rounding to integer; AlphaPopup wired into App.tsx; mobile/tablet block added; @page margin-top:15mm for consistent top margin on new pages; print-block no longer has break-inside:avoid (blocks can split, individual exercises cannot); partial products in multiplication hidden unless showSolutions; answer rows in add/sub and multiplication moved directly below thick line (removed gap row).

**2026-05-27** — Multiple fixes: header writing lines expand to full width; print produces multiple pages (root cause: `overflow: hidden` + `height: 100vh` on html/body/#root overridden in print CSS); cijferen division quotient columns now always match dividend digit count; multiplication Getal 2 mask was overridden by `minMultiplier` clamp — fixed so masked multiplier only clamps to ≥2; print exercise page-break-inside avoidance added at block and exercise level.

**2026-05-26** — Teruggeven config polish: diagram font/size tuned, middle node shows € + blank line in ingevuld mode, Centpatroon updated to Belgian-only options (25ct/10ct/5ct), added "Leeg" scaffolding level (no diagram, just answer field), Prijsbereik moved to preset buttons with auto-updating betalen-met options.

**2026-05-26** — Added Geld > Wissel (bill + drawing box) and Geld > Teruggeven (change-giving arrow diagram with 5 scaffolding levels, 2 answer types) as new active exercise types; renamed "Tekenen" to "Bedrag tekenen" and removed "Gepast betalen" placeholder.

**2026-05-26** — Rewrote CLAUDE.md with full codebase documentation (directory tree, all 8 exercise types, data flow, step-by-step guide for adding new exercise types, code commenting guidelines for junior devs). Created UpdateState.md and added Stop hook to auto-remind about session summaries.
